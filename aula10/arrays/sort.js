var alunos = ["gustavo", "brenda", "nico", "luis", "gabriel"];
console.log(alunos.sort()); // ordem alfabetica

var numeros = [8, 4, 6, 3, 9, 2, 1, 7, 5, 0, 10, 15, 100, ];
console.log(numeros.sort((a, b) => a - b)); // ordem numerica